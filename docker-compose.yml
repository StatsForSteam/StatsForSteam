version: '3'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    depends_on:
      - client
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - 3000:3000
